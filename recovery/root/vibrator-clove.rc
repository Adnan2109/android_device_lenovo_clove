# modules
on early-init
    insmod /vendor/lib/modules/mtk-pwm.ko

service vibrator-clove /vendor/bin/hw/android.hardware.vibrator-service.mediatek
    user root
    group root
    class hal
    seclabel u:r:recovery:s0

on boot
    start vibrator-clove